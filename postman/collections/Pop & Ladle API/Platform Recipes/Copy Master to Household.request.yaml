$kind: http-request
url: "{{baseUrl}}/api/v1/households/{{householdId}}/platform-recipes/{{masterRecipeId}}/copy"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "careRecipientId": "{{careRecipientId}}"
    }
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 26200
