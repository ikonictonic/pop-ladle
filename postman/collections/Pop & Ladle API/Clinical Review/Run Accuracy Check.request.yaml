$kind: http-request
url: "{{baseUrl}}/api/v1/households/{{householdId}}/clinical-review/{{recipeId}}/accuracy-check"
method: POST
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 21300
