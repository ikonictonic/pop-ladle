$kind: http-request
url: "{{baseUrl}}/api/v1/households/{{householdId}}/day-plan/{{dayPlanEntryId}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "status": "completed",
      "acceptance": "partial",
      "notes": "Ate half; refused the side."
    }
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 27200
