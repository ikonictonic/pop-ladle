$kind: http-request
url: "{{baseUrl}}/api/v1/households/{{householdId}}/grocery-list/items/{{groceryItemId}}"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "checked": true
    }
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 28300
