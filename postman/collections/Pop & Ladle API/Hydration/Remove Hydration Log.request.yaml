$kind: http-request
url: "{{baseUrl}}/api/v1/households/{{householdId}}/hydration/{{hydrationLogId}}"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 27700
