$kind: http-request
url: "{{baseUrl}}/api/v1/households/{{householdId}}/care-recipients/{{careRecipientId}}/hydration"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "amountMl": 240,
      "beverage": "water"
    }
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 27600
