$kind: http-request
url: "{{baseUrl}}/api/v1/households/{{householdId}}/care-recipients/{{careRecipientId}}/hydration?date="
method: GET
queryParams:
  date: ""
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 27500
