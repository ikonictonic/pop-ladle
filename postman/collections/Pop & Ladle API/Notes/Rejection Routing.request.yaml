$kind: http-request
url: "{{baseUrl}}/api/v1/households/{{householdId}}/care-recipients/{{careRecipientId}}/rejection-routing"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 29200
