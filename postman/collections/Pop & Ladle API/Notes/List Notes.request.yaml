$kind: http-request
url: "{{baseUrl}}/api/v1/households/{{householdId}}/care-recipients/{{careRecipientId}}/notes?category=&limit=50"
method: GET
queryParams:
  category: ""
  limit: "50"
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 29000
