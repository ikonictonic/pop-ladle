$kind: http-request
url: "{{baseUrl}}/api/v1/invites/accept"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "inviteToken": "{{inviteToken}}"
    }
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 14000
