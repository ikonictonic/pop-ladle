$kind: http-request
url: "{{baseUrl}}/api/v1/households/{{householdId}}/care-recipients?includeArchived=false"
method: GET
queryParams:
  includeArchived: "false"
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 14500

