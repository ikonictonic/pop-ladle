$kind: http-request
url: "{{baseUrl}}/api/v1/admin/households?search=&limit=50"
method: GET
queryParams:
  search: ""
  limit: "50"
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 24200
