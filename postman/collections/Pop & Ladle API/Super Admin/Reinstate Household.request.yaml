$kind: http-request
url: "{{baseUrl}}/api/v1/admin/households/{{householdId}}/reinstate"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "reason": "Dispute resolved."
    }
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 24600
