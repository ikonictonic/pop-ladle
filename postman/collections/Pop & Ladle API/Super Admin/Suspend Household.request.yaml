$kind: http-request
url: "{{baseUrl}}/api/v1/admin/households/{{householdId}}/suspend"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "reason": "Billing dispute under review."
    }
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 24500
