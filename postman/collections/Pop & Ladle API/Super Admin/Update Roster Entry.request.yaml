$kind: http-request
url: "{{baseUrl}}/api/v1/admin/roster/nephrology"
method: PATCH
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "provider": "mock",
      "model": "mock-1",
      "active": true
    }
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 24800
