$kind: http-request
url: "{{baseUrl}}/api/v1/admin/privacy-requests?limit=50"
method: GET
queryParams:
  limit: "50"
auth:
  type: bearer
  credentials:
    token: "{{clerkSessionToken}}"
order: 25100
